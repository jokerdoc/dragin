import util from 'lemon-util';
import Dragin from './dragin.js';

new Dragin(util.getEl('.box'), {
  start(x, y) {

  },
  move(x, y) {
      /* that.x = x;
      that.y = y; */
  },
  end() {

  }
})
